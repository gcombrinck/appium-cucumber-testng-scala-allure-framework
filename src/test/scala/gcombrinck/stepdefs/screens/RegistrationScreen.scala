package gcombrinck.stepdefs.screens

import java.util.concurrent.TimeUnit

import gcombrinck.runner.CucumberRunner
import io.appium.java_client.pagefactory.{AndroidFindBy, WithTimeout}
import io.codearte.jfairy.producer.person.Person
import org.openqa.selenium.WebElement
import org.testng.Assert


@WithTimeout(time = 60, unit = TimeUnit.SECONDS)
class RegistrationScreen extends CucumberRunner{

  }
}
